# limits.yml

# Per-block limits (per island/world). 0 or missing = unlimited.
limits:
  HOPPER: 64

# Per-entity limits (live count in the world). 0 or missing = unlimited.
entities:
  VILLAGER: 50
  IRON_GOLEM: 10
